name: friflex_test
description: Test task for FriFlex
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dio: ^4.0.6
  equatable: ^2.0.3
  bloc: ^8.1.0
  flutter_bloc: ^8.1.1
  shared_preferences: ^2.0.7
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  assets:
    # Добавление в приложение JSON файла с фековыми данным для погоды на 3 дня
    - lib/assets/json/weather_for_3_days.json

  uses-material-design: false
